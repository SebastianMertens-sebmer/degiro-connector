# DEGIRO Trading API v2.0 Dependencies

# Core framework
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20

# Data validation
pydantic==2.11.8

# HTTP client for testing
requests==2.32.3

# DEGIRO connector (install from local or pip)
# degiro-connector>=1.0.0

# Optional: Production dependencies
gunicorn==23.0.0
python-dotenv==1.0.1

# Development dependencies (optional)
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2