# DEGIRO Trading API Environment Configuration

# API Security
TRADING_API_KEY=your-very-secure-api-key-32-characters-minimum

# DEGIRO Configuration
DEGIRO_CONFIG_PATH=config/config.json

# Application Settings
DEBUG=false
LOG_LEVEL=INFO
MAX_WORKERS=4

# Database (if needed for future features)
# DATABASE_URL=postgresql://user:password@localhost:5432/degiro_trading

# Redis (if needed for caching/rate limiting)
# REDIS_URL=redis://localhost:6379/0

# Monitoring (if needed)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# CORS Settings (production)
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000